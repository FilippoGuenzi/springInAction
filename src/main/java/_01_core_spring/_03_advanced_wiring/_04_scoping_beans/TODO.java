package _01_core_spring._03_advanced_wiring._04_scoping_beans;

public interface TODO {
    /**
     * TODO : once we arrive to Spring MVC test injecting all types of session scopes one into the other.
     *
     * Let's try with the StoreService singleton injected by ShoppingCart session beans.
     * Let's try to imagine the other scenarios.
     *
     * TODO : try the difference between ScopedProxyMode.TARGET_CLASS and ScopedProxyMode.INTERFACE
     */
}
