$(
    function(){
        window.app = window.app || {};
        window.app.SpittleView = Backbone.View.extend({
            
        });
        window.app.spittleView = new window.app.SpittleView();
    }
);
